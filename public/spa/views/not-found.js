export function renderNotFound() {
  return `
    <main class="container" style="padding:2rem 0">
      <div class="card">
        <h2>404 — Vista no encontrada</h2>
        <p>La ruta solicitada no existe. Podés volver al inicio desde el menú.</p>
      </div>
    </main>
  `;
}